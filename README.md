# Accident  Detector
The Accident Detector activates a 60-second countdown when triggered. During this time, it retrieves the user's GPS location. If the user cancels, no alert is sent. If they don't respond, the system automatically sends WhatsApp messages to all emergency contacts with the victim's name, exact location, and Google Maps link.
